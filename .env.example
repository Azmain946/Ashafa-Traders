# Optional local overrides (copy to .env if you use one)

DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_TIME_ZONE=Asia/Dhaka

# Optional PostgreSQL (if unset, the app uses db.sqlite3 in the project folder)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pharmacy
